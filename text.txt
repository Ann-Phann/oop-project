? have problem with Cannon::updateBall();

***-------***
basic collision

shape.move(xStep, yStep);
if(shape.getPosition().y > screenHeight - shape.getRadius())
{
    yStep = -yStep;
}

// ammo is not equal to currentLevel
void Cannon::reload(int currentLevel) {
    ammo = currentLevel;
}